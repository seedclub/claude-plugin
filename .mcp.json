{
  "mcpServers": {
    "seed-network": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.js"],
      "env": {
        "SEED_NETWORK_API": "${SEED_NETWORK_API:-https://beta.seedclub.com}",
        "CHROME_PROFILE": "${CHROME_PROFILE:-}",
        "FIREFOX_PROFILE": "${FIREFOX_PROFILE:-}"
      }
    }
  }
}
